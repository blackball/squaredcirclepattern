env = Environment(CCFLAGS="-Wall -O2", LIBS=Split('opencv_core opencv_imgproc opencv_highgui'))
env.Program("test_camera", ["test_camera.cpp", "squaredcirclepattern.cpp"])
env.Program("test_image", ["test_image.cpp", "squaredcirclepattern.cpp"])
